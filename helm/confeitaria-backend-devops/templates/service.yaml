apiVersion: v1
kind: Service
metadata:
  name: {{ include "confeitaria-backend-devops.fullname" . }}
  labels:
    {{- include "confeitaria-backend-devops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "confeitaria-backend-devops.selectorLabels" . | nindent 4 }}
