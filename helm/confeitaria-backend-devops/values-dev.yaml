service:
  type: ClusterIP
  port: 8080
  internalPort: 8080

replicaCount: 1

database:
  user: postgres
  password: root
  host: postgres
  name: confeitariadb

image:
  repository: "confeitaria-backend-devops"
  tag: "local"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: "200m"
    memory: "256Mi"
  requests:
    cpu: "100m"
    memory: "128Mi"