apiVersion: v1
kind: Pod
metadata:
  name: {{ include "confeitaria.fullname" . }}-test-connection
  labels:
    {{ include "confeitaria.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "confeitaria.fullname" . }}-service:{{ .Values.service.port }}']
  restartPolicy: Never
